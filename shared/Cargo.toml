[package]
name = "shared"
version = "0.1.0"
edition = "2021"

[features]
client = []
server = []

[dependencies]
enum_as_f32_macro = { path = "../enum_as_f32_macro" }
enum_mappable = { path = "../enum_mappable" }
bevy = { version = "0.14.1", features = ["dynamic_linking"] }
bevy = { version = "0.15.0", features = ["dynamic_linking"] }
bevy_renet = { version = "0.0.12", features = ["serde"] }
bincode = "1.3.3"
fastrand = "2.3.0"
serde = { version = "1.0.215", features = ["derive"] }
renet_steam = { version = "0.0.2", features = ["bevy"] }
steamworks = { version = "0.11" }
